BoxLayout:
    orientation: "vertical"

    Label:
        id: hello_label
        text: "Hello World"
        on_text: if len(self.text) > 30: self.text = "Click mich!"

    Button:
        text: "Click me!"
        on_release: hello_label.text += "!"
